# hono-preact v0.8.0: release notes (DRAFT)

**Status:** DRAFT — seeded while the work is still unreleased. Flesh out the
Highlights and finalize the Breaking Changes list at release time.

> **How to finalize the breaking-change list.** Generate a public-export-surface
> diff vs the `v0.7.0` tag (the reliable method — do not rely on memory). That
> diff catches *removed/renamed exports* but **misses** breaking changes that
> are (a) the removal of a *member* from an existing exported type, (b) a change
> to an existing export's *type shape*, or (c) a runtime/`engines` requirement.
> Those are recorded manually below (marked ⚠ diff-invisible) so they are not
> lost — the same trap that bit the Babel 8 engines bump.

## Breaking changes

- ⚠ **`LoaderCache.wrap()` removed** (#142). The method was dropped from the
  publicly re-exported `LoaderCache` type (it had no callers and was vestigial).
  Diff-invisible: the `LoaderCache` type name persists, so an export-surface diff
  will not flag it.
- ⚠ **Loader/action data is now typed `Serialize<T>` at the client boundary**
  (#132, #134). `loader.useData()`, `useAction().data`, `useActionResult()`,
  `<Form onSuccess>`, and streaming `onChunk` now reflect the JSON wire shape
  (e.g. a `Date` field arrives as a `string`) instead of the server-side `T`.
  Diff-invisible: the affected exports keep their names; only their result types
  changed. A new `Serialize<T>` type is exported.
- ⚠ **`engines.node` raised to `^22.18.0 || >=24.11.0`** on `hono-preact` and
  `@hono-preact/vite` (#115, Babel 8); the other packages stay `>=20`.
  Diff-invisible: it is a `package.json` engines change, not an export change.
- **`Persist` component removed; persistence is now by-layout** (#133). Live
  streams are consumed through `loader.View`'s accumulating form.
- **Server public API narrowed** (#131). `hono-preact/server` now exports only
  `renderPage` + `HonoContext`/`useHonoContext`; the request handlers
  (`loadersHandler`/`pageActionHandler`) and resolver factories are internal,
  reached by the generated entry via the private `createServerEntry`.
- **`Page` no longer accepts `use` / `location` props** (#97).
- **`RouteParams` no longer claims a param for non-`[A-Za-z0-9_]` names** (#130).
  A hyphenated/dotted segment (e.g. `:foo-bar`) now types as a literal — matching
  the runtime matcher — rather than a required param. Diff-invisible (type-shape
  change to the existing `RouteParams`).

## Highlights (TBD at release time)

The internals quality pass landed the full 12-item comparison backlog plus the
server-API split; notable user-facing pieces to write up: typed-routing rigor,
the `Serialize<T>` wire-mirror, live streams via `loader.View`, the delayed
loader fallback, and the headless Toast primitive.
